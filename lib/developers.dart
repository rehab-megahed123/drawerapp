import 'package:flutter/material.dart';

class Devolopers extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Devolopers"),
      ),
    );
  }
}